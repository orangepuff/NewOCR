# ── Window Sizes ─────────────────────────────────────────────────────────────
# Use FULL to open each window at full screen size, or set a number (pixels).

MAIN_WINDOW_WIDTH=FULL
MAIN_WINDOW_HEIGHT=FULL

OCR_WINDOW_WIDTH=FULL
OCR_WINDOW_HEIGHT=FULL

CROP_PDF_WINDOW_WIDTH=FULL
CROP_PDF_WINDOW_HEIGHT=720    # used only when CROP_PDF_WINDOW_WIDTH is a number

ADD_SPLIT_WINDOW_WIDTH=FULL
ADD_SPLIT_WINDOW_HEIGHT=720   # used only when ADD_SPLIT_WINDOW_WIDTH is a number


# ── OCR ───────────────────────────────────────────────────────────────
# Render scale when rasterising PDF pages for OCR.
# Higher values → sharper images and better accuracy, especially for small or
# italic text. 72 pt/inch base: scale 4.0 ≈ 288 DPI, scale 6.0 ≈ 432 DPI.
# Range: 1.0–8.0. Default: 4.0
OCR_RENDER_SCALE=8.0

# Remove a line from the top N OCR lines if it matches the section title.
OCR_TITLE_MATCH_TOP_LINES=3


# ── UI ───────────────────────────────────────────────────────────────
# Preview-only text scale (%). Does not affect EPUB output.
PREVIEW_TEXT_SCALE_PERCENT=170

PDF_LIST_MIN_HEIGHT=420
OCR_PARAGRAPH_TEXTAREA_MIN_HEIGHT=58


# ── Projects ───────────────────────────────────────────────────────────────
# Default folder for new projects created with the New button.
NEW_PROJECTS_FOLDER=~/Downloads

# ── Email ──────────────────────────────────────────────────────────────────
# Recipient email shown as a "Send Email" button in the EPUB success popup.
# Leave blank to hide the button. Uses Mail (the Mac app) to compose the email.
EPUB_RECIPIENT_EMAIL=orangepuffmufc_HL8GLL@kindle.com
